# 주유 계산기 (fuel-calc)

출퇴근 주유비 지원 정산용. 연비·주행거리·유가를 넣으면 주유소에서 부를 금액을 계산한다.
폰(iOS 사파리)에서 홈 화면에 추가해 쓰는 용도.

## 구조

- `index.html` — 전부. HTML/CSS/JS 단일 파일. 빌드 없음, 의존성 없음.
- 외부 리소스는 폰트 2개(Pretendard, JetBrains Mono)뿐. CDN 실패해도 시스템 폰트로 동작해야 한다.

## 계산 로직

```
거리 = 입력거리 × (왕복이면 2)
리터 = 거리 ÷ 연비
금액 = ceil(리터 × 유가 ÷ 올림단위) × 올림단위
표시 주유량 = 금액 ÷ 유가   ← 올림된 금액 기준으로 다시 계산 (표시 일관성)
```

올림 단위는 1 / 100 / 1000원. 기본값 100원.

## 설계 원칙

- **단일 파일 유지.** 빌드 스텝이나 번들러를 도입하지 않는다. 파일 하나로 어디서든 열려야 한다.
- **모바일 우선.** 한 손 조작, 입력 필드는 `inputmode="decimal"`.
- **저장 실패 허용.** `localStorage`는 try/catch로 감싸고, 실패해도 계산 기능은 정상 동작해야 한다. (`file://`에서 막히는 브라우저 있음)
- LCD 패널은 주유기 디스플레이 모티프. 앰버(#FFBE3D) 금액 + 민트(#5FD9C0) 리터. 이 대비를 유지할 것.
- 숫자는 JetBrains Mono, 한글은 Pretendard.

## 저장되는 값

`localStorage['fuel-prefs']` = `{e: 연비, p: 유가, u: 올림단위, t: 왕복여부}`
주행거리는 매번 달라지므로 저장하지 않는다.

## 배포

GitHub Pages, `main` 브랜치 루트. 푸시하면 1~2분 뒤 반영.

```bash
git add -A && git commit -m "설명" && git push
```

## 앞으로 할 만한 것

- 월별 주행 기록 누적 → 회사 정산용 합계
- 계기판 연비 보정 계수 (실측 대비 5~10% 낙관적인 경향)
- 유가 입력 시 최근값 몇 개 빠른 선택
